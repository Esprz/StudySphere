# Kafka
confluent-kafka

# Utilities
python-dotenv
loguru
jsonschema

# Database
sqlalchemy
psycopg[binary]

# Vector Database
qdrant_client

# Data Processing
pandas
numpy

# Text Processing & Embeddings
# torch is installed separately — see Dockerfile (CUDA) or scripts/setup-local.sh (auto-detect)
sentence-transformers
